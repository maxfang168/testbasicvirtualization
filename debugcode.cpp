#include "install.hpp"
int main()
{
    // This is a debug code to test the installation of the library
    // It should not be used in production
    // It should be removed before releasing the library
_INSTALL_DOT_HPP_CREATE_FILES("computer1");
    return 0;
}